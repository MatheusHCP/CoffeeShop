export type ThemeStructure = {
  colors: {
    background: string
    target: string
    textTarget: string
  },
  spacing: {
    xl: number,
    lg: number,
    md: number,
    sm: number,
    xs: number,
    xxs: number
  },
  staturBar: string,
  fontSize: {
    xl: number,
    lg: number,
    md: number,
    sm: number,
    xs: number,
    xxs: number
  }
}